# Morning briefing — 2026-05-22

**Generated:** 2026-05-22 (smoke-тест project-auditor sub-agent)
**Активных проектов:** 3 (MatricaRMZ, GONBA, setka)
**Запущено через:** 3× параллельных general-purpose агента с system prompt из `.claude/agents/project-auditor.md`

---

## Audit: MatricaRMZ

**Phase:** deep flow
**Last commit:** 2026-05-22 15:18, `087802ca`, v1.21.3: backend auto-fix componentType ↔ карточка номенклатуры
**Last tag:** v1.21.3
**Activity 30d:** ~100 коммитов (поток непрерывный с 2026-04-22, пик 2026-05-18..22)
**Working tree:** clean tracked; 2 untracked — `brain_matrica/` (известная аномалия, заявка #0003) и `docs/inbox-from-brain/` (новый канал из meta-репо)
**Inbox from brain:** 3 заявки (#0001, #0003, #0006) + README

### Текущая нитка (из SESSION_HANDOFF)

Многоэтапный рефакторинг цепочки BOM (`docs/plans/bom-refactor-2026-05.md`, 5 релизов 1.21.0 → 1.22.0). **HANDOFF устарел** — говорит «следующий v1.21.3», фактически v1.21.3 уже выпущен. Реальный следующий — **v1.21.4** (удаление whitelist `BOM_SKELETON_KNOWN_COMPONENT_TYPES`, pre-check дублей, возврат priority в snapshot).

### Открытые техдолги (top-3)

- 🟡 BOM-рефакторинг: v1.21.4 + v1.22.0 (forecast edge cases, audit-скрипт, DDL 0054)
- 🟡 Backend integration-тесты для `upsertWarehouseAssemblyBom` auto-fix
- 🟡 Phase 2.4.4 «финал DROP COLUMN warehouse_id» — ждёт неделю стабильности 1.20.x

### Аномалии

- **SESSION_HANDOFF устарел на один релиз** — нужен `/close_session` для актуализации.
- `brain_matrica/` untracked внутри MatricaRMZ — известно (заявка #0003 уже доставлена). Нужен `.gitignore` или physical move.
- `docs/inbox-from-brain/` untracked — 3 заявки от meta ждут принятия.
- **14 локальных веток `claude/*`** — sub-agent stash, не вычищены. Кандидат на побочную задачу при удобном случае.

### Что для brain_matrica интересно

Образцовая стратегия «большой рефакторинг через mini-bumps» (5-релизный план). Возможная переносимая идея в pool: паттерн `componentTypeByNomenclatureId` header-поле для превентивной UI-диагностики рассинхрона данных перед save. Применим в GONBA если там EAV-подобные связи.

---

## Audit: GONBA

**Phase:** **between threads** (нитка Media→Я.Диск закрыта **сегодня**, ADR-0001 → Implemented, SESSION_HANDOFF не перезаписан)
**Last commit:** 2026-05-22 15:24, `7af4f6b`, Merge PR #29 feat/media-yadisk-finalize
**Last tag:** нет тегов (rolling deploy)
**Activity 30d:** 39 коммитов (бурный спринт 2026-05-18..22)
**Working tree:** clean кроме `?? docs/inbox-from-brain/` (untracked, заявки 0001/0006 + README)
**Inbox from brain:** 2 заявки + README

### Текущая нитка (из SESSION_HANDOFF)

SESSION_HANDOFF говорит «Phase 0+1+2 пройдены, PR1 #24 на ревью, дальше PR2…PR5», но фактически **все 5 PR (#24-#29) уже смержены сегодня**, ADR-0001 закрыт коммитом `b247663`. **Handoff устарел — нитка завершена**, следующая не назначена.

### Открытые техдолги (top-3)

- 🟡 **`authorized_keys` на GONBA-сервере содержит ключи `valstan@a6fd55b8e0ae` (MatricaRMZ) и `valstan@setka`** — цепочка компрометации. **Критично** — см. заявку #0007.
- 🟢 Rename Media-документа после Phase 3 ломает автозаливку (нужен `moveYandexResource` в afterChange)
- 🟢 62 orphan-файла в `public/media` без записи в БД + `web/scripts/yadisk-sync-media.ts` не приведён к phase-3-семантике

### Аномалии

- `docs/inbox-from-brain/` untracked.
- SESSION_HANDOFF рассинхронизирован с git log: говорит «PR1 на ревью», по факту нитка домержена.
- Нет ни одного git tag — релизы каскадом без версионирования (CI deploy by merge to main). Сознательный выбор, не дефект.

### Что для brain_matrica интересно

GONBA — самый активный проект (39 коммитов за 4 дня, deep flow до сегодняшнего полудня). Идея #003 (SESSION_HANDOFF + `/close_session`) уже применена и **сразу выявила свою слабость** — handoff не самообновляется при «нитка дозакрыта внутри сессии». Это переносимая боль во все три проекта (см. заявку #0006 «Failed approaches» уже косвенно адресует).

GONBA сейчас **в окне `between threads`** — самое время для приёма входящих заявок.

---

## Audit: setka

**Phase:** **deep flow** (big idea «модуль авто-регистрации регионов» MVP только что приземлён, 21 staged file без коммита)
**Last commit:** 2026-05-22 14:51, `bbf551e`, fix(notifications): like_comment uses user-token only (VK error 3 Unknown method)
**Last tag:** нет тегов
**Activity 30d:** 38 коммитов (взрывной всплеск 2026-05-21/22 — ~24 за двое суток)
**Working tree:** **грязное** — 21 staged файл (миграция 011 + `modules/discovery/{vk_search,ai_categorizer}.py` + Celery `tasks/discovery_tasks.py` + `web/api/discovery.py` + 4 теста + 2 темплейта + JS + обновления `models.py`/`main.py`/`base.html`/DEV_HISTORY/PENDING) + untracked `docs/inbox-from-brain/`
**Inbox from brain:** 5 заявок (0001, 0002, 0004, 0005, 0006) + README

### Текущая нитка (из DEV_HISTORY свежее сверху)

**Big idea «модуль авто-регистрации регионов и сообществ (MVP)»** — запись 2026-05-22 сверху, описывает миграцию 011 + `modules/discovery/{vk_search,ai_categorizer}` + Celery таску + wizard `/regions/new` + UI `/regions/<code>/discovery` + **60 новых тестов (330/330 зелёных)**. Всё сидит **staged без коммита** — нитка не закрыта, `/reliz` ещё не отработал.

### Открытые техдолги (top-3)

- ⏳ `recheck_existing_communities()` — Celery beat weekly + Telegram-alert (вторая итерация big idea)
- 🟡 Хардкоды `/home/valstan/SETKA/logs/parser*` в `web/api/parsing.py` и `tasks/parsing_tasks.py` (вынести в env)
- 🟢 Мигрировать `web/api/publisher.py` на extended VKPublisher

### Аномалии

- **Фаза не «between threads» как мы предполагали** — big idea уже взята, MVP реализован, 21 файл staged без коммита. **Реестр brain_matrica был неверен на момент создания.**
- 5 заявок в inbox — превышение лимита, и **пришли в плохое время** (deep flow). Брейн стоит снизить активность к setka.
- Stale ветка `claude/community-tokens-everywhere` (с `+` маркером).

### Что для brain_matrica интересно

**setka НЕ в окне для побочных задач.** Лёгкие brain-заявки (0001/0002/0004/0005/0006) лучше **придержать** до завершения текущей нитки (`/reliz` MVP big idea). Включить в SESSION_HANDOFF brain_matrica: «следующий цикл — не отправлять setka новые заявки».

**Образцовый паттерн setka:** `DEV_HISTORY свежее сверху` + детальные блоки изменений каждой большой итерации. Это противоположный паттерн от MatricaRMZ/GONBA (SESSION_HANDOFF + `/close_session`). Перевернутая интерпретация заявки #0004: **GONBA скорее должна взять у setka DEV_HISTORY-подобный паттерн**, а не наоборот. Это требует пересмотра.

---

## Dispatch state

- **📤 active sent:** 6 заявок (0001-0006). Стартовая партия — превышен лимит у setka.
- **✅ closed since last briefing:** 0 (это первый briefing).
- **⛔ rejected since last briefing:** 0.

## GitHub open PRs

(не запущено для smoke-теста — `gh pr list` опускается для скорости. В реальном briefing'е добавляется.)

## Phase changes triggered

| Проект | Было | Стало | Причина |
|---|---|---|---|
| GONBA | PoC mode | **between threads** | Media→Я.Диск нитка закрыта (PR #24-#29 смержены 2026-05-22) |
| setka | between threads | **deep flow** | Big idea MVP взят, 21 staged file, релиз впереди |
| MatricaRMZ | deep flow | deep flow (без изменений) | BOM-refactor продолжается, v1.21.3 выпущен, v1.21.4 next |

## Что заслуживает внимания

1. **🔴 SECURITY (новая заявка #0007):** в `authorized_keys` GONBA-сервера лежат ключи `valstan@a6fd55b8e0ae` (MatricaRMZ) и `valstan@setka`. Это цепочка компрометации — если скомпрометирован любой из этих хостов, захвачен и GONBA. Изоляция SSH-ключей применена per-deploy (идея #001), но **исторические ключи остались**. Срочная задача для GONBA-сессии.

2. **HANDOFF-устаревание во всех трёх:** все три проекта имеют SESSION_HANDOFF / DEV_HISTORY рассинхронизированный с git log. Закономерность, не случайность — нитка закрывается внутри сессии, а `/close_session` или `/finish` не вызывается потому что «сессия еще идёт». Это **системный недостаток паттерна #003**, не локальная боль. Возможно — идея в pool «handoff надо обновлять не только при выходе из сессии».

3. **Стоп-лист setka подтверждён** — пользователь в deep flow, не отвлекать. Никаких новых заявок туда до закрытия минимум двух текущих.

4. **GONBA в `between threads`** — окно для приёма входящих заявок (2 в inbox), для рассмотрения новых заявок, для побочных задач. Может быть подходящее время взять #0001 (унификация имени команды) первым делом.

5. **MatricaRMZ — переносимая идея для pool**: паттерн «componentType in nomenclature header field для превентивной UI-диагностики».

6. **Технический долг по `claude/*` веткам в MatricaRMZ** — 14 stale веток. Не блокер, но кандидат на побочную чистку.

---

## Sources

- Аудиты получены через 3× параллельных `Agent(subagent_type="general-purpose", ...)` с system prompt из `.claude/agents/project-auditor.md`.
- В реальной brain_matrica-сессии (где `project-auditor` зарегистрирован как кастомный subagent) — вызов будет короче: `Agent(subagent_type="project-auditor", prompt="audit <path>")`.
- Кастомный subagent подхватывается **при следующем старте** Claude Code сессии.
