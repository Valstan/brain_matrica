# Заявка #0004: Применить SESSION_HANDOFF в setka (DEV_HISTORY на усмотрение)

> 🧠 **Заявка от brain_matrica.** Сформулирована meta-сессией, у которой нет полного контекста этого проекта (видит только git log, CLAUDE.md, SESSION_HANDOFF, PENDING_FOLLOWUPS — не видит код, тесты, work-in-progress).
>
> Это **предложение, не задача.** Проанализируй с точки зрения текущего состояния проекта: подходит ли структурно, не дублирует ли уже существующее, не утяжелит ли, есть ли побочные эффекты. Можешь отклонить без объяснений или вернуть встречное «вот как лучше».
>
> Источник: brain_matrica session 2026-05-22. **Обновлено** 2026-05-22 — пользователь зафиксировал позицию «нитка между сессиями нужна везде».

**Target:** setka
**Created:** 2026-05-22
**Updated:** 2026-05-22 (уточнение по DEV_HISTORY)
**Status:** sent
**Связанные идеи pool:** [#003](../../cross-project-ideas/ideas/003-session-handoff.md)
**Связано:** #0001 (имя команды → `/close_session`)

## Решение пользователя (2026-05-22)

**SESSION_HANDOFF.md — обязательно** во всех трёх проектах. Цитата: «надо чтобы было везде нитка между сессиями».

**DEV_HISTORY.md** — на усмотрение setka. Цитата: «дев-хистори не знаю нужна ли, у нас вроде и так в гитхабе в комитах все записано основное». Brain_matrica приводит аргументы за/против, но не настаивает.

## Что обязательно

### 1. Добавить `docs/SESSION_HANDOFF.md` в setka

Формат как у MatricaRMZ / GONBA / brain_matrica. Минимум:

```markdown
# setka — Session Handoff

**Status:** ACTIVE | IDLE
**Updated:** YYYY-MM-DD
**Branch:** main

## Текущая нитка
1-3 предложения: что в работе **сейчас**, на каком этапе.

## Следующий шаг
Конкретно: что делать в следующей сессии.

## Контекст
- План (если есть): `docs/plans/X.md`
- Связанные коммиты: <hashes>
- Failed approaches (см. #0006): <если есть>

## Открытые вопросы для пользователя
- <если есть>

## Не забыть (low-priority)
```

### 2. Обновить `/close_session` skill setka

После переименования (по #0001) `close_session.md` должен:
- Обновлять `docs/SESSION_HANDOFF.md` (новое поведение)
- Опционально — проверять что DEV_HISTORY обновлён (если оставите)
- Делать `git commit + push` отдельным коммитом про handoff

См. реализацию в MatricaRMZ `.claude/commands/close_session.md` / GONBA `.claude/commands/close_session.md` — копируем структуру, наполнение специфично для setka.

### 3. `/start` skill setka — добавить «шаг 0»

Читать `docs/SESSION_HANDOFF.md` **первым**:
- `Status: ACTIVE` + Updated не старше 14 дней → выделить нитку в шапке отчёта, спросить «Продолжаем?»
- `Status: IDLE` → пропустить, переходить дальше
- Если устарел → «handoff устарел, возможно не актуален»

## Что — на усмотрение (DEV_HISTORY)

**Аргументы оставить DEV_HISTORY:**
- У вас он образцовый по формату (project-auditor это отметил): свежее сверху, детальные блоки итераций, цифры тестов
- При **больших** релизах (когда несколько коммитов сворачивают в одну фичу) — DEV_HISTORY читать проще чем `git log`
- Связь с PENDING_FOLLOWUPS (закрытое перетекает сюда) — рабочая

**Аргументы упразднить DEV_HISTORY:**
- У вас хорошие commit messages — много пересекается
- SESSION_HANDOFF + ADR + git log + Failed approaches (#0006) — комбинация покрывает 95% задач
- Меньше документов поддерживать

**Brain_matrica не настаивает.** Если оставляете — отлично, проект-auditor подтвердил что формат образцовый. Если убираете — заявка #0006 (Failed approaches) **становится критичной** компенсацией: уроки переезжают туда.

## Что записать обратно

В `docs/DEV_HISTORY.md` (если оставляете) или новый `docs/SESSION_HANDOFF.md`:

```markdown
### 2026-XX-XX — brain_matrica dispatch

- `#0004` (SESSION_HANDOFF в setka): applied. Создан `docs/SESSION_HANDOFF.md`, обновлён `/close_session` skill (после переименования из `/finish`), `/start` читает SESSION_HANDOFF шагом 0.
- DEV_HISTORY: **оставлен** (формат образцовый, продолжаем) / **упразднён** (контент покрыт SESSION_HANDOFF + git log + Failed approaches).
- Обновлена строка #003 в `cross-project-ideas/INDEX.md` brain_matrica: setka → `✅ 2026-XX-XX`.
```

После — **удалить этот файл** из `docs/inbox-from-brain/`.

## Когда делать

setka сейчас в `deep flow`. **Рекомендация**: после `/reliz` MVP — когда фаза станет `between threads`. Тогда переход безопасный.

Если хочется сделать **сразу** SESSION_HANDOFF (просто создать файл и заполнить под текущую нитку MVP) — это **не блокирует** работу, и даже **полезно**: 21 staged file без коммита — идеальный кейс «нужна нить, чтобы не забыть на каком шаге MVP остановился».
