# Заявка #0005: `LOG_PATH` env var вместо hardcoded пути логов в setka

> 🧠 **Заявка от brain_matrica.** Сформулирована meta-сессией, у которой нет полного контекста этого проекта (видит только git log, CLAUDE.md, SESSION_HANDOFF, PENDING_FOLLOWUPS — не видит код, тесты, work-in-progress).
>
> Это **предложение, не задача.** Проанализируй с точки зрения текущего состояния проекта: подходит ли структурно, не дублирует ли уже существующее, не утяжелит ли, есть ли побочные эффекты. Можешь отклонить без объяснений или вернуть встречное «вот как лучше».
>
> Источник: brain_matrica session 2026-05-22.

**Target:** setka
**Created:** 2026-05-22
**Status:** sent

## Контекст у brain_matrica

Видел в `setka/docs/PENDING_FOLLOWUPS.md` запись (приоритет 🟡, секция «Запуск и окружение»):

> **`main.py:25` хардкодит `/home/valstan/SETKA/logs/app.log`.** Локально на Windows запустить приложение нельзя — только тесты. Стоит вынести путь в env (`LOG_PATH`) с дефолтом на прод-значение.

Также в git log 2026-05-22 видел коммит `c7bc45f`: `chore(dx): pre-commit on py3.11 + LOG_PATH env + setup-dev scripts` — то есть **возможно уже сделано**, но запись в PENDING_FOLLOWUPS осталась (или сделано частично).

Чего я **не** видел:
- Содержимое коммита `c7bc45f` (что именно сделано — добавлен LOG_PATH или только setup-dev scripts)
- Текущее состояние `main.py:25` — действительно ли там ещё hardcoded путь
- Тесты которые могли бы покрывать эту логику

## Предложение

В setka-сессии:

1. Проверить: уже ли `LOG_PATH` реализован (коммит `c7bc45f`)? Если **да** — закрыть запись в `PENDING_FOLLOWUPS.md` и перенести в `DEV_HISTORY.md` как done.
2. Если **нет** или **частично** — доделать:
   - В `main.py:25` (и везде где hardcoded) заменить на `os.environ.get('LOG_PATH', '/home/valstan/SETKA/logs/app.log')`.
   - На проде в `/etc/setka/setka.env` — добавить `LOG_PATH=/home/valstan/SETKA/logs/app.log` (если хочется явно) или оставить fallback на default.
   - Локально для Windows-разработки можно установить `$env:LOG_PATH = "C:\Users\valstan\setka-dev\app.log"` либо относительный `./logs/app.log`.
   - Тест: smoke-test `python -c "import main"` локально не должен падать на отсутствии `/home/valstan/SETKA/logs/`.

## Зачем

- **Сейчас невозможно** запустить `main.py` локально на Windows — приходится использовать только pytest. Это разрыв в DX, видный из `CLAUDE.md` setka («`main.py` локально не запускать»).
- Низко-контекстная **техническая зачистка** — не блокирует ни одну активную нить, можно вклинить в перерыв между большими задачами.

## Возможные «нет»

- Если `c7bc45f` уже всё закрыл — задача отпала, нужно просто обновить PENDING_FOLLOWUPS.
- Если в setka есть **архитектурное решение** «main.py работает только на проде, локально только pytest» — тогда LOG_PATH env не нужен.

## Что записать обратно

В `docs/DEV_HISTORY.md`:

```markdown
### 2026-XX-XX — техдолги

- `brain_matrica dispatch #0005`: `LOG_PATH` env var — <уже сделано в коммите c7bc45f, закрыли запись / доделали, теперь main.py запускается локально / отклонили: <причина>>.
```

И обновить `docs/PENDING_FOLLOWUPS.md`:
- Если done — удалить запись из 🟡 секции «Запуск и окружение».
